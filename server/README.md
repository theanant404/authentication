1. Package.json `npm init -y` for insilling the project 
2. tsconfig.json `tsc --init` for insilling typescritp conficuration 
3. `npm install @types/node` for avlable for node and support type script 
#### how to run a ts 
1. `tsc app.ts node app.js`
2. `ts-node app.ts`
3. `nodemon app.ts (server)`
4. `npm start`